#include <stdio.h>

/**
 * main - adrees of variables
 *
 * Return : Always 0.
 */

int main(void)
{
	char c;
	char n;

	printf("Address of variable 'c': %p\n", &c);
	printf("Address of variable 'n': %p\n", &c);
	return (0);
}
